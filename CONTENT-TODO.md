# Content still needed from the pastor

The site is live with placeholder content in the spots below. Send the real
content whenever it's ready and it can be swapped in directly.

- **Service times** — Sunday service time(s) and any weekday meetings
  (`src/pages/visit/`, `src/pages/ne/visit/`)
- **Directions/map pin** — confirm the meeting location matches
  Dhumbarahi-4, Kathmandu, or provide the correct address
- **Pastor & leadership bios + photos** (`src/pages/about/`)
- **Ministries** — real names, descriptions, schedules, leaders to replace
  the example categories (`src/content/ministries/`)
- **Sermons** — audio/video links or notes, and how they should be hosted
  (`src/content/sermons/` — currently empty)
- **Giving / donation method** — bank details, eSewa/Khalti, or an
  off-site giving link (`src/pages/giving/`)
- **Nepali translations** of the Statement of Faith and Church Covenant
  (`src/content/faith/ne.md`, `src/content/covenant/ne.md`) — the original
  Nepali constitution document was typed in a legacy non-Unicode font and
  can't be reused as-is; it needs to be re-typed in Unicode Nepali
- **Logo / brand colors** — none were provided; the site currently uses a
  plain text wordmark and neutral colors
